import { decode } from 'base64-arraybuffer';
import { supabase } from '~/lib/supabase';

export const locationService = {
  // services: editing location/photos from existing journey
  // services: deleting location/photos from existing journey
  // services: uploading location/photos to existing journey
};
