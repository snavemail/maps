import { View, Text } from 'react-native';
import React from 'react';

export default function LocationPreview({ location }: { location: any }) {
  return (
    <View>
      <Text>LocationPreview</Text>
    </View>
  );
}
